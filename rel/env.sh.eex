#!/bin/sh

# Local-only distribution — allows `bin/kerf remote` to attach.
# sname keeps the node off the network (no full DNS name, no epmd on 0.0.0.0).
export RELEASE_DISTRIBUTION=sname
export RELEASE_NODE=kerf

# Crash dumps written next to the release binary (not /tmp).
export ERL_CRASH_DUMP="${RELEASE_ROOT}/erl_crash.dump"

# IEx shell history for `bin/kerf remote` sessions.
export ERL_AFLAGS="-kernel shell_history enabled"
