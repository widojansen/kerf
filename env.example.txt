# ExClaw Docker Environment Variables
# Rename to .env and fill in required values:
#   mv env.example.txt .env.example
#   cp .env.example .env

# --- Required ---
# Generate with: mix phx.gen.secret  (or: openssl rand -base64 48)
SECRET_KEY_BASE=

# --- Database ---
DB_USER=wido
DB_PASS=
# DB_HOST=localhost      (default, fine with --network=host)
# DB_PORT=5432           (default)
# DB_NAME=exclaw_prod    (default)

# --- LLM Backend ---
# Options: "vllm", "ollama", or leave empty for Anthropic-only
LLM_BACKEND=vllm
VLLM_URL=http://localhost:8000
VLLM_MODEL=nvidia/Qwen3-32B-NVFP4
EXCLAW_DEFAULT_MODEL=nvidia/Qwen3-32B-NVFP4
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=qwen3:8b
# ANTHROPIC_API_KEY=sk-ant-...

# --- Embeddings ---
EMBEDDING_URL=http://localhost:11434
EMBEDDING_MODEL=bge-m3

# --- Knowledge Base Embeddings ---
# Defaults to EMBEDDING_URL/EMBEDDING_MODEL if not set.
# KB_EMBEDDING_URL=http://localhost:11434
# KB_EMBEDDING_MODEL=bge-m3

# --- Email Triage (Phase B) ---
# Requires Gmail OAuth credential stored in CredentialVault.
# EMAIL_TRIAGE_ENABLED=true
# EMAIL_POLL_INTERVAL_MS=300000            (default: 5 minutes)
# GMAIL_CREDENTIAL_NAME=gmail_oauth        (default)
# CLASSIFICATION_MODEL=nvidia/Qwen3-32B-NVFP4

# --- Channels ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOW_FROM=

# --- Services ---
# SEARXNG_URL=http://localhost:8080       (default)
# CLICKHOUSE_URL=http://localhost:8123
# TELEGRAM_ALERT_CHAT_ID=                 (defaults to first TELEGRAM_ALLOW_FROM)
# DASHBOARD_IP=127.0.0.1                  (Bandit bind address)
