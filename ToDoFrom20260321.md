**Immediate (today/tomorrow):**

1. **Telegram bot setup** — create with @BotFather, test end-to-end on the Spark
2. **Update CLAUDE.md** — add VLLMProvider and Telegram module contracts so Claude Code has the full picture
3. **OpenClaw → vLLM migration** — Anita is still hitting Ollama. Point OpenClaw's Ollama config at vLLM's OpenAI-compatible endpoint (should be a URL change since vLLM speaks the same format)

**This week:**

1. **vLLM systemd hardening** — the service is enabled but we should verify it survives reboot cleanly, and consider running ExClaw itself as a systemd service (always-on Telegram bot)
2. **Anita's scripts → vLLM** — `health_monitor.py`, `incoming_mail.py`, `monitoring_query.py` all call Ollama at `:11434`. Update them to call vLLM at `:8000` with OpenAI format for concurrent access
3. **Unsloth fine-tuning setup** — this was on the roadmap right after OpenClaw/ExClaw. Now that vLLM is running, you can fine-tune Qwen or Nemotron for your specific domains (automotive, Dutch invoices) and serve the LoRA adapters through vLLM

**Near-term:**

1. **ExClaw MCP bridge** — the IronClaw-MCP pattern you noted: expose agent memory, job management, and supervision status to Claude Code via stdio/HTTP
2. **Blog series** — document the vLLM migration, it's a strong article: "From Ollama to vLLM: 16x Throughput on a DGX Spark"
3. **Second model in vLLM** — currently serving only Qwen3-32B-NVFP4. Consider adding a fast model (Nemotron-3-Nano or Qwen3-8B-NVFP4) for routing — quick tasks go to the small model, complex reasoning to the 32B

**On the horizon:**

1. **Phase 2 hardware** — second DGX Spark for 256GB cluster via ConnectX-7
2. **Agency launch** — ExClaw targeting production readiness as the milestone