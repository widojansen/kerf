# ExClaw Status Report — 2026-02-20

## Project Health

| Metric | Value |
|--------|-------|
| **Tests** | 367 tests + 1 doctest, **0 failures** (12 docker tests excluded) |
| **Compiler warnings** | **0** (compiles clean with `--warnings-as-errors`) |
| **Test speed** | 4.4 seconds total |
| **Source files** | 49 `.ex` files, ~5,200 lines |
| **Test files** | 32 `.exs` files, ~5,200 lines |
| **Test:Code ratio** | ~1:1 |

## Phases Complete (11 of 11 scoped)

| Phase | Module | Tests |
|-------|--------|-------|
| 1. Security Layer | FileGuard, ShellSandbox, PromptGuard | 61 |
| 2. LLM Provider | Provider, RateLimiter | 28 |
| 3. Agent Session | Session, Supervisor | 23 |
| 4. Memory Store | Store, Fact, Message | 34 |
| 5. CLI Channel | CLI, Mix task | 18 |
| 6. Scheduler | Scheduler, schemas | 28 |
| 7. Dashboard | EventLog, LiveView | 22 |
| 8. Telemetry | Logger, ClickHouse/JSONL | 23 |
| 9. Container | Manager, Supervisor | 18 + 12 integration |
| 10. WhatsApp | Channel, bridge.js | 24 |
| 11. Tool Registry | Registry, WebFetch, WebSearch | 87 |

## Architecture vs Implementation — Gap Analysis

### Implementation Order: Planned vs Actual

| # | Architecture Plan | What Was Built | Status |
|---|-------------------|---------------|--------|
| 1 | Security layer | Phase 1 — Security | Matches |
| 2 | LLM Provider | Phase 2 — LLM | Matches |
| 3 | Agent Session | Phase 3 — Agent | Matches |
| 4 | Memory Store | Phase 4 — Memory | Matches |
| 5 | CLI Channel | Phase 5 — CLI | Matches |
| 6 | **Telegram Channel** | **Skipped** — WhatsApp done instead (Phase 10) | **Diverged** |
| 7 | Scheduler | Phase 6 — Scheduler | Reordered earlier |
| 8 | Dashboard | Phase 7 — Dashboard | Reordered earlier |
| 9 | WhatsApp Channel | Phase 10 — WhatsApp | Done, order swapped with Telegram |
| 10 | Docker sandboxing | Phase 9 — Container | Done, reordered |
| — | *Not in plan* | Phase 8 — Telemetry (ClickHouse) | **Added** |
| — | *Not in plan* | Phase 11 — Tool Registry + WebFetch + WebSearch | **Added** |

### Modules in the Architecture Not Yet Built

| Module | Architecture Description | Status |
|--------|------------------------|--------|
| `ExClaw.Config` | Agent holding runtime config from JSON | **Not built** — config uses standard `Application.get_env` instead |
| `ExClaw.Channels.Telegram` | Polling-based Telegram adapter | **Not built** — listed as "future phase" in CLAUDE.md |
| `ExClaw.Channels.Supervisor` | Umbrella supervisor for all channels | **Not built** — WhatsApp has its own supervisor, CLI isn't a GenServer |
| `ExClaw.Tools.Supervisor` | Supervisor for all tools | **Not built** — Tools are plain modules, Registry is under Application |
| `ExClaw.Tools.DynamicSupervisor` | Custom skills loaded at runtime | **Not built** |

### Design Decisions That Diverged (Improvements)

| Area | Architecture Said | What Was Built | Verdict |
|------|-------------------|---------------|---------|
| **Config** | `ExClaw.Config` Agent GenServer | Standard `Application.get_env` | Simpler, idiomatic Elixir |
| **CLI** | GenServer | Plain module with blocking REPL | Correct — agent loop is already a GenServer |
| **Tools** | GenServers per tool | Plain modules dispatched via Registry | Better — tools are stateless functions, no need for GenServer overhead |
| **WebSearch** | Brave API | SearXNG (self-hosted) | Better — no API key dependency, privacy |
| **LLM Provider** | "Anthropic, OpenRouter" | Anthropic only | Narrower scope, sufficient for now |
| **Scheduler** | Quantum library mentioned | Pure `Process.send_after` | Better — no external dependency for simple scheduling |
| **Mocking** | Mox listed in deps | Adapter injection pattern | Better — no test-only dependency needed |
| **Telemetry** | Not planned | ClickHouse + JSONL fallback | Valuable addition for observability |
| **Tool Registry** | Static registry | ETS-backed dynamic registry | More extensible |

### What's Missing to Complete the Architecture

1. **Telegram channel** — The only planned component not built. The architecture has a full sketch with polling, `fetch_updates`, and authorized sender filtering. WhatsApp proves the pattern works.

2. **Dynamic tool loading** — The architecture envisioned `Tools.DynamicSupervisor` for "custom skills loaded at runtime." The ETS-backed Registry already supports `register_tool/2` at runtime, so the foundation exists — but there's no plugin discovery/loading mechanism.

3. **Multi-provider LLM** — The architecture mentioned OpenRouter alongside Anthropic. Currently Anthropic-only.

### Summary

The project has **delivered more than the architecture planned** (telemetry, dynamic tool registry, web tools, SSRF protection) while making **pragmatic simplifications** (no unnecessary GenServers for stateless tools, no Config Agent, no Mox). The one gap is the **Telegram channel**, which was the originally planned "first real channel" but got deprioritized in favor of WhatsApp. The architecture's core thesis — OTP supervision, per-group isolation, security-first — is fully realized.